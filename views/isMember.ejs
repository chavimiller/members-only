<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/css/main.css" />
    <title>Membership Code</title>
  </head>
  <body>
    <div class="container section">
      <% if (error === 'wrongCode') { %>
      <p class="msg-error">Incorrect membership code, try again.</p>
      <% } %>
      <h1>Do you have a member code?</h1>
      <h2>
        Have a club code? Enter your secret code. If not, you can always join
        later!
      </h2>
      <form action="/auth/member" method="POST" autocomplete="off">
        <input name="code" type="text" placeholder="Enter member code" />
        <button>Validate</button>
      </form>
      <div class="lower-line"><a class="redirect" href="/home">Cancel</a></div>
    </div>
  </body>
</html>
